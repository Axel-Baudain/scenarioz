import React from 'react';
import './style.css';

const SearchBar = () => (
  <div className="searchbar">
    <i class="fas fa-search searchbar__icon" />
    <input className="searchbar__input" />
  </div>
);

export default SearchBar;